# JWT로 REST API에 보안 적용하기

REST API 를 보호하기 위해서는 아래와 같은 기능 필요

- 보안이 적용된 API 에는 JWT 없이 액세스 할 수 없다.
- JWT는 로그인이나 가입과정 또는 리프레시(refresh) 토큰을 사용해 만들 수 있다.
- 유효한 사용자가 사용자 이름과 비밀번호를 사용해 로그인하거나 유효한 사용자가 가입하는 경우에 한해서만 JWT 와 리프레시 토큰을 제공해야 한다.
- 비밀번호는 `bcrypt`의 강력한 해싱 기능을 사용해 인코딩한 후 저장해야 한다.
- JWT는 `RSA`를 사용해 생성한 키로 서명해야 한다.
- 페이로드으 ㅣ클레임에는 민감한 정보나 보안 정보를 저장해선 안된다. 이러한 정보를 저장하려면 암호화해야 한다.
- 특정 역할의 사용자에게 API 접근 권한을 부여할 수 있어야 한다.

## 새로운 API 추가

- 회원가입, 로그인, 로그아웃, 토큰 리프레시 API 추가해 기존 API 개선
- 토큰 리프레시
    - 기존 토큰이 만료되면 리프레시 토큰을 사용해 새 액세스 토큰(JWT) 제공
    - 이러한 이유로 회원 가입/로그인 API의 응답에는 액세스 토큰과 리프레시 토큰이라는 두 가지 유형의 토큰 정보가 포함돼 전달
    - JWT 액세스 토큰은 자체 만료되므로 로그아웃 중에는 리프레시 토큰만 제거하면 됨.

### Sign-up (가입)

- `openapi.yaml` 에 `/api/v1/users` 추가
- `SignedInUser` 모델에 (`openapi.yaml` 에 정의) accessToken, refreshToken, username, userId 필드 추가

### Sign-in (로그인)

- `openapi.yaml` 에 `/api/v1/auth/token` 추가 (post method)
- 요청 페이로드를 처리할 새 모델인 `SignInReq` 추가 (username, password)

### Sign-out (로그 아웃)

- `openapi.yaml` 에 delete method 로 `/api/v1/auth/token` 추가
- sign-out 요청을 받으면 사용자의 리프레시 토큰 제거
- 이 경우에는 토큰에서 사용자 ID를 가져온 다음 해당 ID 를 사용해 USER_TOKEN 테이블에서 ID 를 제거하면 됨.
- 이 엔드포인트에 접근할때 유효한 엑세스 토큰 필요
- 예제에서는 페이로드에 삭제할 토큰을 담아서 보내는 방법 사용
- 따라서 `openapi.yaml` 에 `RefreshToekn` 모델 추가함.

### 토큰 리프레시

- `openapi.yaml` 에 `/api/v1/auth/token/refresh` 추가
- 이 명세는 리프레시 토큰 리소스를 표현하는 URI 형태를 갖고 있지만 예외적인 형태로 구성
- 이상적으로는 이 URI에 POST 호출을 전송하면 URI에 정의된 새로운 리소스 (리프레시 토큰)를 생성해야 함.
- 하지만 이 엔드포인트는 리프레시 토큰 대신에 엑세스 토큰을 생성하고 `SignedInUser` 응답객체에 담아서 전달.
- 리프레시 토큰은 테이블로 관리 (`V1.0.0__Init.sql` 의 `user_token` 테이블) 

# JWT 관리자 구현

## 상수 선언

- `Constants.java` 에 보안 기능과 관련된 모든 상수 선언
- `XPIRATION_TIME = 900_000` 는 시간 값으로 **15분**을 나타냄

## JWT 관리자 정의

- `JwtManager.java` 에 JWT 관리자 클래스 정의
- JwtManager 는 새 JWT 을 생성하는 커스텀 클래스로 auth0.com 에서 제공하는 java-jwt 라이브러리 사용
- 토큰 서명을 위해선 공개/비밀 키를 사용해야 함.
- JWT 클래스는 토큰 생성을 위한 플루언트(fluent) API 를 제공하는 java-jwt 라이브러리 클래스이며, 이 클래스를 이용해 발행자(ISS), 주제(sub), 발행일(iat) 및 만료일(exp) 클레임 추가
- 또한 `UserDetails` 에 명시된 권한을 사용해 생성되는 커스텀 클레임인 `ROLE_CLAIMS(roles)`를 추가
  - `UserDetails` 는 스프링 시큐리티가 제공하는 인터페이스
  - org.springframework.security.core.userdetails.User.builder() 메소드를 사용해 UserBuilder 클래스를 만들 수 있음
  - `UserBuilder` 는 `UserDetails` 인스턴스를 생성하는 최종 빌더 클래스임.
- 마지막 부분에서 `JwtManager.create()` 메소드가 `sign()` 작업을 실행하는데, 이 작업은 공개, 개인 RSA 키와 함꼐 `SHA256withRSA` 알고리즘을 사용해 JWT 를 서명함.
  - JWT 헤더는 알고리즘(alg) 클레임에 대해서 HS256 값을 명시함.

## 공개 키/개인 키 생성

- 서명은 RSA 알고리즘으로 생성한 공개 키와 개인키를 사용해 수행됨.
- JDK 의 keytool을 사용해 키 저장소를 만들고 공개/개인 키를 생성할 수 있음.
  - 공개/개인 키는 생성된 후 90일 동안만 유효

```bash
$ keytool -genkey -alias "jwt-sign-key" -keyalg RSA -keystore jwt-keystore.jks -keysize 4096
```

- 생성된 키 저장소는 `src/main/resources` 디렉토리 아래에 있어야 함.
- **keytool** 명령에 사용되는 필수 값은 아래와 같이 application.properties 파일에도 명시해야 함.

```properties
app.security.jwt.keystore-location=jwt-keystore.jks
app.security.jwt.keystore-password=password
app.security.jwt.key-alias=jwt-sign-key
app.security.jwt.private-key-passphrase=password
```

